{
  "name": "web",
  "version": "0.0.1",
  "description": "Rudimentary web interface for minecraft server",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --ignore '*.test.js' --ignore '__mocks__/*' --ignore 'public/*' --unhandled-rejections=strict",
    "test": "nodemon --ignore 'public/*' --exec jest",
    "lint": "nodemon --exec eslint --fix ."
  },
  "author": "https://github.com/FrankDeGroot",
  "repository": "https://github.com/FrankDeGroot/mcweb",
  "license": "MIT",
  "dependencies": {
    "bufferutil": "^4.0.1",
    "nodemon": "^2.0.4",
    "rcon": "^1.0.3",
    "socket.io": "^2.3.0",
    "utf-8-validate": "^5.0.2"
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1",
    "jest": "^26.4.1"
  }
}
